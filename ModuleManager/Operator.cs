using System;

namespace ModuleManager
{
    public enum Operator
    {
        Assign,
        Add,
        Subtract,
        Multiply,
        Divide,
        Exponentiate,
        RegexReplace,
    }
}
